import { Wrench, type LucideIcon } from "lucide-react";

/**
 * Default icon for Services (admin sidebar “Add Services”, etc.).
 * Uses Lucide `Wrench` so it matches other nav stroke icons.
 */
export const ServiceIcon: LucideIcon = Wrench;
