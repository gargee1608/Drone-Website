import type { ReactNode } from "react";

/** Fonts for this route come from `landingFontClassName` in `ServicesView` (same as Marketplace). */
export default function ServicesLayout({ children }: { children: ReactNode }) {
  return children;
}
