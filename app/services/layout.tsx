import type { ReactNode } from "react";

/** Fonts from `landingFontClassName` in `ServicesView`; header/footer match other landing routes. */
export default function ServicesLayout({ children }: { children: ReactNode }) {
  return children;
}
