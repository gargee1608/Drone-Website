import type { ReactNode } from "react";

/** Fonts from `landingFontClassName` in `ServicesView`; header/footer match other landing routes (e.g. Marketplace). */
export default function ServicesLayout({ children }: { children: ReactNode }) {
  return children;
}
